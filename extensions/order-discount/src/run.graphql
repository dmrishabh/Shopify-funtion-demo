query RunInput {
  cart {
    cost {
      totalAmount {
        amount
        currencyCode
      }
    }
    lines {
      id
    }
  }
  discountNode {
    metafield(namespace: "custom-order-discount", key: "discount-values") {
      value
    }
  }
}
